import { Injectable } from '@angular/core';
import { clients } from './client-grid/client-list';

@Injectable({
  providedIn: 'root',
})
export class ClientsService {
  constructor() {}

  // @ts-ignore
  clients;

  /* getClients(limit: number) {
    while  
  } */
}
